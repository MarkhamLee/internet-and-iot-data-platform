## Site Monitor Agent Archive Files

The original iteration of this agent had content ingestion, reminders and research in one pipeline, and they've since been split out into separate pipelines. As a result there are large number of redundant or unused files in the folders for each pipeline that need to be removed, rather than delete them all, I'm putting them aside here for future reference, in case they're needed, etc.
